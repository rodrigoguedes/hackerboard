<%= form_with url: login_path do |f| %>
  <p>
    <%= f.label :email, t("form.labels.login.email") %>
    <%= f.email_field :email %>
  </p>

  <p>
    <%= f.label :password, t("form.labels.login.password") %>
    <%= f.password_field :password %>
  </p>

  <p>
    <%= f.submit t("form.submit.login") %>
  </p>
<% end %>